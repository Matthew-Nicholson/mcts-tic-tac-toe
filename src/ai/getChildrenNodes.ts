import { BoardState } from "../shared/types/boardState";
import { Node } from "./node";

export function getChildrenNodes(node: Node<BoardState>): Node<BoardState>[] {
  // TODO @Matthew-Nicholson
  return [];
}
